version: '2.1'

services:
  ng2admin:
    build: .
    ports:
      - '2999:2999'
    volumes:
      - './ng2-admin/src:/var/www/src'
    working_dir: '/var/www'
    command: 'bash -c /var/www/run-dev.sh'
